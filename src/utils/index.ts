import { getNewBoard, randomNumber } from './random/random';
import {
  shuffle,
  moveCell,
  canMoveСell,
  isSolved,
  moveInDirection,
} from './common/common';

export {
  moveInDirection,
  getNewBoard,
  randomNumber,
  shuffle,
  moveCell,
  canMoveСell,
  isSolved,
};
